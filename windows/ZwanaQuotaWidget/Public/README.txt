The AppExtension contract's PublicFolder: the folder a host is allowed to read
from the package. AppxManifest.xml declares it as PublicFolder="Public", and a
declared folder that does not exist in the package is a registration the host
may reject. Nothing is published through it yet, so this file is here to make
the folder real.
