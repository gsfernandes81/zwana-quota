# Credentials — the zwana portal login, read by zwana_quota.py
.env

__pycache__/
*.pyc

# uv-managed dev environment (make dev)
.venv/
.pytest_cache/

# mutation testing (make mutants)
.poodle-temp/
.hypothesis/

# Android build output (android/) -- CI builds the APK, see android/README.md
android/**/.gradle/
android/**/build/
android/.kotlin/
android/**/.kotlin/
android/local.properties
*.keystore
*.jks

# Connect IQ build output and the developer key (garmin/)
garmin/bin/
garmin/*.der
garmin/*.pem
